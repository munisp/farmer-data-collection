[package]
name = "isobus-gateway"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "isobus-gateway"
path = "main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
