[package]
name = "tokenization-service"
version = "0.1.0"
edition = "2021"
description = "Commodity tokenization and CBDC integration for FarmConnect"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
